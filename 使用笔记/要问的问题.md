# 测试中的 moving_line 问题
根据`README.md`说明，测试模型需要执行的代码为：`python test_our_policy.py --vis_html --plot_loop --moving_line --model_dir logs/debug`。但是，执行时收到报错
```sh
usage: Parse configuration file [-h] [--config CONFIG] [-m MODEL_DIR] [--phase PHASE] [--debug] [--gpu_id GPU_ID] [--gpu] [--randomseed RANDOMSEED] [--vis_html] [--plot_loop]
                                [-d PLANNING_DEPTH] [-w PLANNING_WIDTH] [--sparse_search]
Parse configuration file: error: unrecognized arguments: --moving_line
```
很明显，原因是 test_out_policy.py 中根本就没有`moving_line`这个参数。实际代码及 git commit 中也确实没有发现相应内容。

接下来删除`moving_line`参数执行
